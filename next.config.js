/** @type {import('next').NextConfig} */
const nextConfig = {
  // CSP disabled temporarily to allow stream playback
  // TODO: Re-enable with proper directives once streaming is working
};

module.exports = nextConfig;
