# Memory — 2026-08-05

## 07:01 UTC — Pre-compaction flush

- User (Fabian) had extensive conversations across July 31 – August 5 working on GLST (Guestlist iOS app + web dashboard).
- **iOS project path**: `/Users/fabiandubz/Documents/IOS APPS EXPORTED/GLST/GLST/`
- **Web app path**: `/Users/fabiandubz/Documents/The Guestlist/glst-dashboard/`
- Migrated iOS streaming from Cloudflare WHIP/RTMP to **LiveKit** after repeated failures (WHIP→HLS impossible, RTMP disconnects after ~36s).
- LiveKit Cloud project: `guestlist-tv`, URL `wss://guestlist-tv-ei1a8q8r.livekit.cloud`
- iOS LiveKit streaming confirmed working (connects, publishes camera/audio tracks).
- Web app AWS Amplify deployment ongoing — LiveKit player + token endpoint added.
- Xcode build warning logged via openclaw-tui: `Duplicate definition of category 'RTCDevice' on interface 'UIDevice'` in WebRTC framework headers. Likely harmless but worth noting.
- User preference: small tested changes, backup before major edits, careful approach.
- Frustration with approval timeouts noted — prefers direct action where safe.
- Multiple backup branches created on GitHub (`LiveKitBirth`, `profiles`, `livekit-integration`).

