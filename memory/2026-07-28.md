# 2026-07-28 - Session Memory

## Guestlist.tv Slideshow Layout Fix
- **Issue**: Slideshow videos stacked above/below text on mobile web
- **Fix**: Changed `EcosystemHeroBanner.tsx` to always use `flex-row` instead of `flex-col md:flex-row`
- **Video sizes**: 24px wide mobile, 32px desktop
- **Hide**: "GUESTLIST ECOSYSTEM" text hidden on mobile

## Avatar Upload Fix
- **Issue**: Upload failed with "Upload failed: Failed to upload"
- **Fix**: Updated `/api/upload-avatar/route.ts` to:
  - Auto-create `dj-avatars` bucket if missing
  - Convert File to Buffer before upload (HaishinKit compatibility)
  - Better error logging with console.error
  - Send 500 response with error details
- **Profile save fix**: `/api/dj-profile/route.ts` now includes `avatar_url` field
- **Web profile page**: Uses API instead of direct Supabase (bypasses RLS)

## iOS Streaming Button Fix
- **Issue**: START STREAMING button just spun with no action
- **Fix**: BroadcastView now calls actual streaming functions:
  - `onStartBroadcast` closure to call `startBroadcast()`
  - `startStreaming()` called when tapping START STREAMING
  - `stopStreaming()` called when tapping STOP STREAM
- **Screen lock**: Added `UIApplicationIdleTimerDisabled` to Info.plist
- **GO LIVE button**: Now actually starts streaming (not just shows camera view)

## Cloudflare Stream API
- Endpoint: `/api/cloudflare/rtmp/route.ts`
- Returns RTMP URL and stream key for mobile app
- Fallback credentials hardcoded in iOS app if API fails

## Deployment
- Git commits: `ec7674b` (slideshow), `703b4c6` (avatar), pending `e887873e` (iOS streaming)
- AWS Amplify: Auto-deployed from GitHub main branch
- iOS: User builds in Xcode manually
