# Memory Flush — 2026-07-27

## Guestlist.tv Slideshow Logo Placement
- User wants PadPro and RMT logos placed at the **bottom-right corner** of the slideshow (not beside it, not split to opposite corners).
- Screenshot reference: `/Users/fabiandubz/Downloads/Screenshot 2026-07-27 at 18.32.21.jpg` shows red box at bottom-right of slideshow indicating desired logo placement.
- User explicitly said: "Put the logo animations of PadPro and RateMyTransition where the red box is" — both logos together in that corner.
- Current code has logos outside slideshow as separate `<a>` tags with `flex justify-between` layout.
- Needs to be moved inside slideshow container as `absolute bottom-1 right-1 z-20 flex gap-1.5` with both logos together.
- User declined the edit for now ("no don't worry about doing that it's okay") but this is the intended placement when ready.

## Current Guestlist.tv Status
- Slideshow has 3 slides: PadPro video, RMT video, dark perks slide.
- Rotation interval: 10 seconds.
- Transition duration: 1.5s.
- Logos currently outside slideshow at 50px, pushed to edges with `justify-between`.
- Need to: move logos inside slideshow container, group them bottom-right at ~26px each.

## Build Status
- Last push: amplify.yml fix (SUPABASE_SERVICE_ROLE_KEY typo), colorful channels, perks slideshow, DJ profile, stream scheduling.
- Build results pending — user hasn't confirmed if latest deployment succeeded.

## User Preference
- **Not comfortable with Terminal** — prefers direct file edits by assistant.
- **Can approve commands when needed** but gets frustrated by repeated approval requests.
- **Prefers GUI/TextEdit** over Terminal scripts for project changes.
- **Wants single URL** (guestlist.tv) for all traffic.

## Next Steps (When User Resumes)
1. Fix logo placement inside slideshow at bottom-right corner.
2. Verify latest AWS Amplify deployment builds successfully.
3. Implement stream recording/archive system (item #5 in user's requested order).
4. Continue with remaining tasks from 2026-07-26.md summary.
